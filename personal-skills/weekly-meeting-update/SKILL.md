---
name: weekly-meeting-update
description: |
  주간 회의록의 담당 섹션을 이번주 실제 작업 기반으로 자동 생성한다.
  트리거 키워드: "주간회의록", "회의록 업무 정리", "회의록 업데이트", "/weekly-meeting-update".
  작업 영역을 파라미터로 받으며 기본값은 첫 번째 서비스.
  이번주 커밋·HANDOFF 문서를 취합해 금주/차주 계획 블록을 회의록 마크다운으로 출력한다.
---

> ⚠️ **설치 후 필수**: 이 파일의 `<개인_노트_경로>`를 실제 노트 경로로 교체해야 정상 동작한다.
> 교체 방법: `README.md`의 "개인 스킬에 노트 경로 연결" 섹션 참조.

# Weekly Meeting Update

주간 회의록 중 **담당 서비스 섹션**을 이번주 실제 작업 기반으로 생성한다.  
회의록 전체가 아닌 **담당 서비스 블록만** 출력한다.

## 워크플로우

### Step 1 — 이번주 월요일 계산

```bash
# 노트 경로 설정 확인 — 미설정이면 중단
if echo "<개인_노트_경로>" | grep -q '<개인_노트_경로>'; then
  echo "⚠ [설정 필요] ~/.claude/skills/weekly-meeting-update/SKILL.md 에서 <개인_노트_경로>를 실제 경로로 교체하세요."
  echo "  교체 후 스킬을 다시 실행해주세요. (참조: README.md '개인 스킬에 노트 경로 연결' 섹션)"
  exit 1
fi

python3 -c "
from datetime import date, timedelta
today = date.today()
monday = today - timedelta(days=today.weekday())
print(monday.isoformat())
"
```

### Step 2 — 데이터 수집 (3개 병렬 실행)

| 항목 | 명령 / 경로 |
|------|------------|
| **HANDOFF_NOW.md** | `<서비스명>/docs/status/HANDOFF_NOW.md` 읽기 |
| **HANDOFF.md** | `<서비스명>/docs/plans/HANDOFF.md` 읽기 |
| **이번주 커밋** | `git log --oneline --since="<MONDAY>" -- <서비스명>/` |

다른 서비스 영역이면 경로를 해당 서비스 경로로 교체한다.

커밋 메시지에서 `#숫자` 패턴으로 이슈 번호를 추출한다.  
이슈 번호가 없는 커밋(문서, 설정 변경 등)은 내용으로 판단해 적절히 분류한다.

### Step 3 — 작업 분류

HANDOFF 세션 내역 + 커밋을 아래 카테고리로 분류한다.  
해당 주에 없는 카테고리는 생략한다.

| 카테고리 | 포함 기준 |
|----------|----------|
| QA 대응 및 버그 수정 | `[QA]` 이슈, 버그 픽스, 잔버그 처리 |
| 기능 개발 | 신규 기능, API 연동, 컴포넌트 신규 구현 |
| 기획 QA 반영 | 기획 QA, UX 흐름 변경 |
| 디자인 QA 반영 | 디자인 QA, 스타일 수정, 디자인 토큰 적용 |
| 배포 / 인프라 | 배포 설정, 서버 설정, CI/CD, 환경 변수 |

카테고리가 1~2개뿐이면 카테고리 헤더 없이 플랫 리스트로 출력해도 된다.

### Step 4 — 출력

아래 형식으로 출력한다.

```markdown
#### 금주 계획 (MM/DD ~ MM/DD)

**[카테고리명]**
- [작업 내용 요약] ([#번호](https://github.com/<조직명>/<레포명>/issues/번호))
- [작업 내용 요약] (이슈 없을 경우 생략)

**[카테고리명]**
- ...

#### 차주 계획

- [예정 작업 내용]
- [예정 작업 내용]

담당: @<담당자>
```

핵심 규칙:
- 이슈 번호 → 반드시 GitHub 링크: `[#번호](https://github.com/<조직명>/<레포명>/issues/번호)`
- **금주 계획**: 이번주 실제 완료 작업 (카테고리별 그룹)
- **차주 계획**: HANDOFF_NOW.md §2 다음 작업 기반
- 담당자: `@<담당자>` (git user: <git_user>)
- 동일 이슈가 여러 커밋/세션에 걸쳐 있으면 한 줄로 합쳐서 표현한다
- HANDOFF.md 세션 내역이 커밋보다 상세하므로 HANDOFF 내역을 우선 활용한다
- 카테고리가 1~2개뿐이면 카테고리 헤더 없이 플랫 리스트로 출력해도 된다
