---
tags: [하네스, 공용, 구축, claude-code]
updated: 2026-04-28
---

# 공용 하네스 구축 가이드

어느 회사·프로젝트에서나 Claude Code 하네스를 처음 설치할 때 사용하는 범용 가이드.

> **배포 단위**: `하네스/` 폴더 전체가 패키지다.
> 새 환경에 `하네스/` 폴더를 복사하고 `install.sh`를 실행하면 스킬·설정이 복원된다.

---

## 하네스 폴더 구조

```
하네스/
  install.sh               ← 부트스트랩 스크립트
  skills/                  ← 프로젝트 워크플로우 스킬 (항상 설치)
    project-init/SKILL.md
    project-fix/SKILL.md
    project-pr/SKILL.md
    project-issue/SKILL.md
    session-close/SKILL.md
    document-review/SKILL.md
  personal-skills/         ← 개인 생산성 스킬 (설치 시 선택)
    daily-note/SKILL.md
    standup/SKILL.md
    meeting-note/SKILL.md
    til/SKILL.md
    weekly-retro/SKILL.md
    weekly-meeting-update/SKILL.md
  hooks/                   ← 훅 스크립트 (--hooks 플래그로 설치)
    pre-commit-doc-check.sh
    check-source-doc.sh
  Claude Code/             ← 개요·체크리스트·메모리 가이드
  설정_템플릿/              ← settings.json·MCP 템플릿
  운영_가이드/              ← HANDOFF 규칙·세션 패턴
  프롬프트/                 ← 서브에이전트 호출 패턴·caveman 압축 프롬프트
  공용_하네스_구축_가이드.md ← 이 문서
```

---

## 새 개발 환경 시나리오

### Step 0 — 사전 준비: Claude Code CLI 설치

`claude` 명령이 없으면 install.sh 첫 단계에서 즉시 중단된다. 먼저 설치한다.

```bash
# npm으로 설치 (Node.js 18 이상 필요)
npm install -g @anthropic-ai/claude-code

# 설치 확인
claude --version
```

> Claude Code 공식 설치 가이드: https://docs.anthropic.com/ko/claude-code/getting-started

### Step 1 — 하네스 폴더 확보

```bash
# 방법 A: 담당자에게 폴더를 전달받은 경우 — 작업 경로로 복사
# (전달받은 위치에서 직접 실행해도 무방. 복사는 원본 보존 목적)
cp -r 하네스/ ~/dev-harness/

# 방법 B: git clone (레포 URL은 배포 후 공개 예정)
# TODO: 배포 전 아래 URL을 실제 레포 주소로 교체할 것
# git clone https://github.com/<owner>/<repo> 하네스
```

### Step 2 — 부트스트랩 실행

하네스 폴더가 있는 위치에서 실행한다.

```bash
bash 하네스/install.sh
```

실행 흐름:
1. 의존성 확인 (`claude`, `python3`, `git`, `gh`)
2. `하네스/skills/` → `~/.claude/skills/` 복사 (프로젝트 스킬, 항상)
3. 개인 생산성 스킬 설치 여부 대화형으로 질문 (`하네스/personal-skills/`)
4. `~/.claude/settings.json` 생성 (model 설정 + additionalDirectories)
   - 외부 경로 필요 시 대화형으로 입력받음

개인 스킬을 미리 포함하려면:
```bash
bash 하네스/install.sh --personal
```

외부 경로와 함께 지정하려면:
```bash
bash 하네스/install.sh --personal --dir /path/to/external/dir
```

FE/풀스택 프로젝트에서 FE 문서 자동 감지 훅을 설치하려면:
```bash
bash 하네스/install.sh --hooks
```

> **훅 설치 후**: `/project-init` 실행 시 소스 경로를 자동 분석해 `.claude/hooks-config.sh`를
> 생성한다. 수동 조정이 필요하면 `Claude Code/훅_스크립트_전문.md` 참조.

> **gh 인증 — `project-fix` · `project-pr` · `project-issue` 사용 전 필수**
>
> `gh`가 설치되어 있어도 인증이 없으면 GitHub 스킬이 모두 실패한다.
> install.sh 실행 후 아래 명령으로 인증한다.
>
> ```bash
> gh auth login
> # 브라우저 인증 또는 Personal Access Token 입력
> gh auth status   # 인증 확인
> ```

> **개인 스킬 설치 — 노트 경로 자동 설정**
>
> `install.sh --personal` 실행 중 노트 경로를 자동으로 묻고 치환한다.
> 경로를 입력하면 `daily-note`, `standup`, `meeting-note`, `til`, `weekly-retro`, `weekly-meeting-update` 스킬의
> `<개인_노트_경로>` 플레이스홀더를 자동으로 교체한다.
> 노트 시스템이 없으면 엔터로 건너뛰어도 된다 (해당 스킬은 비활성화 상태로 유지).
>
> 설치 시 건너뛴 경우 수동 설정:
> ```bash
> NOTE_PATH="/실제/노트/경로"   # 예: /Users/홍길동/Documents/Dev-Vault/개인
>
> for skill in daily-note standup meeting-note til weekly-retro weekly-meeting-update; do
>   FILE=~/.claude/skills/$skill/SKILL.md
>   [ -f "$FILE" ] && sed -i.bak "s|<개인_노트_경로>|$NOTE_PATH|g" "$FILE" && rm -f "${FILE}.bak" && echo "✓ $skill"
> done
> ```

### Step 3 — 프로젝트 하네스 구축

```bash
cd ~/프로젝트
claude   # Claude Code 실행
```

Claude에게:
```
/project-init
```

자동 실행:
- 코드베이스 구조 파악 (언어·프레임워크·모노레포 여부)
- Q&A 수집 (서비스명·FE/BE/풀스택·CLAUDE.md 공유 여부·에이전트 위치·브랜치전략·외부경로 등 최대 8개)
- `CLAUDE.md` 생성
- `docs/<서비스명>/status/HANDOFF_NOW.md` + `docs/<서비스명>/plans/HANDOFF.md` + `docs/<서비스명>/history/세션_노트.md` 초기화

> **Q&A 작성 팁**
>
> - **Q5 브랜치 전략**: 팀 규칙을 모르면 팀장에게 먼저 확인한다. 예시: `"develop 브랜치 기준, task/<이슈번호> 형식"`
> - **Q6 외부 접근 경로**: install.sh에서 이미 하네스 폴더를 등록했으므로 보통 `"없음"`. 별도 문서 저장소(Vault 등)가 있으면 해당 경로를 입력.

### Step 4 — 검증

Claude에게 작업 시작 메시지를 보내면 CLAUDE.md STEP 0-B가 자동으로 HANDOFF_NOW.md를 읽고 요약 출력한다:

```
이 프로젝트 작업 시작할게.
```

아래와 같이 `§1 현재 상태`와 `§2 다음 작업` 섹션이 자동 출력되면 구축 완료:

```
§1 현재 상태: 브랜치 main, 이슈 없음, 빌드 npm run dev
§2 다음 작업: 첫 이슈 생성 후 개발 착수
```

내용이 비어 있거나 파일을 찾지 못하면 Step 3을 다시 실행한다.

---

## BE 프로젝트 빠른 시작 (FE 없는 순수 백엔드)

FastAPI·Go 등 FE가 없는 BE 전용 프로젝트에 하네스를 구축할 때의 Q&A 참고 예시다.

### `/project-init` Q&A 예시 — FastAPI 서비스

| 질문 | 예시 답변 |
|------|----------|
| Q1 서비스명 | `user-service` |
| Q2 FE/BE/풀스택 | `BE` |
| Q3 주요 언어·프레임워크 | `Python / FastAPI / SQLAlchemy` |
| Q4 모노레포 여부 | `단일 레포` 또는 `모노레포 — services/user-service` |
| Q5 브랜치 전략 | `develop 기준, task/<이슈번호>` |
| Q6 외부 접근 경로 | `없음` (하네스 폴더는 이미 등록됨) |
| Q7 커밋 Co-author | `Claude Sonnet 4.6` |

### `/project-init` Q&A 예시 — Go 서비스

| 질문 | 예시 답변 |
|------|----------|
| Q1 서비스명 | `api-gateway` |
| Q2 FE/BE/풀스택 | `BE` |
| Q3 주요 언어·프레임워크 | `Go / Gin / GORM` |
| Q4 모노레포 여부 | `모노레포 — services/api-gateway` |
| Q5 브랜치 전략 | `main 기준, feat/<이슈번호>` |

### BE 전용 `hooks-config.sh` 예시

FE 패턴 없이 BE 패턴만 설정:

```bash
# .claude/hooks-config.sh
# check-source-doc.sh 변수
BE_PATTERN="services/user-service/(routes|services|handlers|models)/([^/]+)"
BE_DOC_BASE="docs/pages"
BE_PROJECT="user-service"
# pre-commit-doc-check.sh 변수
BE_COMMIT_PATTERN="^services/user-service/.*\.(py|go)$"
BE_DOMAIN_EXTRACT='s|services/user-service/routes/([^/]+)/.*|\1|; s|services/user-service/services/([^/]+)/.*|\1|'
BE_DOC_DIR_BASE="docs/pages"

# FE 없음 — FE 패턴은 빈 문자열로 비활성화
FE_PATTERN=""
```

### 생성되는 HANDOFF_NOW.md 구조 (BE 전용)

```markdown
## §1 현재 상태

- 서비스: user-service (FastAPI / Python 3.11)
- 브랜치: develop
- 마지막 작업: 초기 구축

## §2 다음 작업

- 첫 이슈 생성 후 개발 착수
```

### BE 개발 중 에이전트 활용 패턴

| 상황 | 에이전트 |
|------|---------|
| API 엔드포인트 추가·수정 | 일반 작업 (또는 프로젝트 dev 에이전트) |
| DB 스키마·마이그레이션 | `architecture` 검토 후 구현 |
| 단위 테스트 작성 | `test-writer` |
| 성능·쿼리 이슈 | `debugger` |
| OpenAPI 스펙·문서 | `doc-writer` |

---

## 구축 후 일상 워크플로

| 상황 | 스킬 |
|------|------|
| 매일 아침 | `/daily-note` |
| 스탠드업 전 | `/standup` |
| 이슈 생성 | `/project-issue` |
| 버그·QA 수정 시작 | `/project-fix` |
| PR 생성 | `/project-pr` |
| 세션 종료 | `/session-close` |
| 금요일 회고 | `/weekly-retro` |
| 회의 후 | `/meeting-note` |
| 배운 것 정리 | `/til` |

---

## 관련 문서

- [settings.json 템플릿](설정_템플릿/settings.json_템플릿.md) — settings.json 전체 구조
- [MCP 설정 템플릿](설정_템플릿/mcp_설정_템플릿.md) — MCP 서버 등록
- [코딩 원칙](운영_가이드/코딩_원칙.md) — Claude Code 작업 4원칙 (Think Before Coding · Simplicity · Surgical · Goal-Driven)
- [수동 구축 절차](운영_가이드/수동_구축_절차.md) — install.sh 사용 불가 환경의 Phase 1~4 수동 부트스트랩
- [스킬 업데이트 규칙](운영_가이드/스킬_업데이트_규칙.md) — 하네스 소스 ↔ ~/.claude/skills/ 양방향 동기화
- [Caveman 압축 프롬프트](프롬프트/caveman_프롬프트.md) — 응답 토큰 60~75% 절감 단일 프롬프트
- [HANDOFF 유지 규칙](운영_가이드/HANDOFF_유지_규칙.md) — HANDOFF 비대화 방지
- [세션 패턴 모음](운영_가이드/세션_패턴_모음.md) — 작업 유형별 패턴
- [UI 검증 전략](운영_가이드/UI_검증_전략.md) — Playwright MCP 토큰 최적화 · 3단계 검증 흐름
- [트러블슈팅](Claude Code/트러블슈팅.md) — 설치·훅·/project-init 오류 해결
